apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - hpa.yaml
# secret.yaml is intentionally not listed - copy secret.yaml.example to
# secret.yaml with real values and apply it separately.
